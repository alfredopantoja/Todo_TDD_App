<h1>Tasks</h1>
<%= form_for @task do |f| %>
	<%= f.label :task %>
	<%= f.text_field :task %>
	<%= f.submit %>	
<% end %>

<ul>
	<% @tasks.each do |task| %>
		<li>
			<%= task.task %> | <%= link_to 'Edit', edit_task_path(task) %>
		</li>
	<% end %>
</ul>		
